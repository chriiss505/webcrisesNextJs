{
  "name": "web-crises",
  "version": "0.1.0",
  "private": true,
 "scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "export": "next export",
  "deploy": "gh-pages -d out",
  "build-and-deploy": "npm run build && npm run export && npm run deploy"
},
  "dependencies": {
    "framer-motion": "^12.5.0",
    "next": "15.2.3",
    "path": "^0.12.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sass": "^1.86.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
